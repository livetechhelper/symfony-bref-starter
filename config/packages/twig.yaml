twig:
    file_name_pattern: '*.twig'
    default_path: '%kernel.project_dir%/templates'
    form_themes:
        - 'tailwind_2_layout.html.twig'
        - 'form/tailwind_customizations.html.twig'
    globals:
        # Add any global variables here

when@test:
    twig:
        strict_variables: true
