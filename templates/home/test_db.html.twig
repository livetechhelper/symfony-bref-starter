{% extends 'base.html.twig' %}

{% block title %}Welcome to Symfony Bref Starter!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello there! ✅</h1>

    <p>If you're using a database and everything is configured correctly, you should see 2 test widgets displayed below</p>

    {% if error is not empty %}

        <div>
            <p>There was an error trying to load the widgets from your database...</p>
            <pre>
                {{ dump(error) }}
            </pre>
        </div>

    {% else %}

        <div>
            <ul>
                {% for widget in widgets %}
                    <li><b>ID: {{ widget.id }}</b> - {{ widget.name }}</li>
                {% endfor %}
            </ul>
        </div>

    {% endif %}





</div>
{% endblock %}
